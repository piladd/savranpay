{
  "name": "savranpay-vercel-wrapper",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix ../../savranpay-web ci && npm --prefix ../../savranpay-web run build && node copy-dist.mjs"
  }
}
