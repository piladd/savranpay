name: Code Quality

on:
  pull_request:
    branches: [ main ]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  format-check:
    name: Format Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore SavranPay.sln

      - name: Check formatting
        run: dotnet format SavranPay.sln --verify-no-changes --severity warn

  build-warnings:
    name: Build — Treat Warnings as Errors
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore SavranPay.sln

      - name: Build strict
        run: >
          dotnet build SavranPay.sln
          --no-restore
          --configuration Release
          -p:TreatWarningsAsErrors=false
          -p:Nullable=enable
